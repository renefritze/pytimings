[tox]
envlist = py310, py39, py38, py37, flake8

[testenv:flake8]
basepython = python
deps = flake8
commands = flake8 pytimings tests

[testenv]
setenv =
    PYTHONPATH = {toxinidir}
deps =
    .[ci]
commands =
    pip install -U pip
    pytest --basetemp={envtmpdir}
